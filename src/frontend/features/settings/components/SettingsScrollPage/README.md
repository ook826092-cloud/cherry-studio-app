# SettingsScrollPage

This settings-local component family owns the common page shell used by child pages in the settings
tree.

It composes the common route header with an optional fixed search surface and scrolling content.
Navigation destinations, screen state, persistence, and translations remain in the consuming
child page.
