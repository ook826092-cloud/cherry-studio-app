# SettingsServiceRow

This settings-local component family owns the dense service row used by provider and MCP pages.

`SettingsGroupedSeparator` is private implementation detail for grouped rows. Service state,
navigation, actions, and translations remain in the consuming child page.
