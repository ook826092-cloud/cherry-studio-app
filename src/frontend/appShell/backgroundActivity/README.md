# Background Activity

This App Shell module owns the platform-specific Live Activity factories registered during app
bootstrap. They are process-level presentation adapters rather than page components.
